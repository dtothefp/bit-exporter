{
  "name": "bit-exporter",
  "devDependencies": {
    "@commitlint/cli": "15.0.0",
    "@commitlint/config-conventional": "15.0.0",
    "husky": "7.0.4"
  },
  "scripts": {
    "prepare": "echo \"prepare\" && husky install",
    "postinstall": "echo \"postinstall\" && husky install"
  }
}
